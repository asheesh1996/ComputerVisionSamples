@inherits LayoutComponentBase

<FluentToastProvider />
<FluentDialogProvider />
<FluentTooltipProvider />
<FluentMessageBarProvider />
<FluentMenuProvider />
<FluentDesignTheme Mode=mode OfficeColor=officeColor />

<div>
    @Body
</div>

@code {
    // Default theme settings, adjust as needed
    private DesignThemeModes mode { get; set; } = DesignThemeModes.Dark;
    private OfficeColor? officeColor { get; set; } = OfficeColor.Office;
}
